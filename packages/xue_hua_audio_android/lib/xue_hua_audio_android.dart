/// Android implementation of the `xue_hua_audio` plugin.
///
/// This package contains only native (Kotlin) code. The Dart side of the
/// plugin lives in `xue_hua_audio_platform_interface`, whose default
/// method-channel implementation talks to the native code in this package.
///
/// `xue_hua_audio` 插件的 Android 实现。
///
/// 本包只包含原生（Kotlin）代码；插件的 Dart 端逻辑位于
/// `xue_hua_audio_platform_interface`，其默认的 MethodChannel 实现
/// 会与本包中的原生代码通信。
library;
