/// iOS and macOS implementation of the `xue_hua_audio` plugin.
///
/// This package contains only native (Swift) code shared between iOS and
/// macOS. The Dart side of the plugin lives in
/// `xue_hua_audio_platform_interface`, whose default method-channel
/// implementation talks to the native code in this package.
///
/// `xue_hua_audio` 插件的 iOS 与 macOS 实现。
///
/// 本包只包含 iOS/macOS 共享的原生（Swift）代码；插件的 Dart 端逻辑位于
/// `xue_hua_audio_platform_interface`，其默认的 MethodChannel 实现
/// 会与本包中的原生代码通信。
library;
